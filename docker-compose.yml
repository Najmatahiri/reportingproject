services:
  web:
    build: .
    command: python3 /code/manage.py runserver 0.0.0.0:8000
    env_file:
      - .env.test
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres:16
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"
